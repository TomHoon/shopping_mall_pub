# 1. 의존성 설치

```aiignore
npm install -D husky
npm install -D lint-staged
```

# 2. 허스키 초기화

```aiignore
npx husky init
```

# 3. package.json lint-staged 추가

```aiignore
...

  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "format": "prettier --write .",
    "lint": "eslint",
    "prepare": "husky"
  },
  // 아래 추가
  "lint-staged": {
    "*.{js,ts,tsx,json,css,md}": [
      "prettier --write"
    ]
  },

...
```

# 4. husky shell script 추가

```aiignore
[pre-commit.sh]

npx lint-staged
```
